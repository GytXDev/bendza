{
    "installCommand": "npm install --omit=optional false",
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}